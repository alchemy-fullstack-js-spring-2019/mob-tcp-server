// ryan's example:
// module.exports = str => {
//   if(str[0] !== '0') {
//     return null;
//   }
// @all hi
//@dm:ryan hi
//@nick:ryan

// const pattern =  regex expression
// const match = pattern.exec(str)

// if match or match groups does not == then return null,
// or else return object command key, (match.groups.command)
//                       message key,
//                       args (text) key

// }

// takes a string message and returns a command object 
// (what is the command and any parameters) based on the 
// contents of the message.

function parseMessage(aString) {
  
}
module.exports = parseMessage;
