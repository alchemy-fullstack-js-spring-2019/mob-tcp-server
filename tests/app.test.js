const ChatRoom = require('../lib/app');

const chatRoom = new ChatRoom();

describe('chatroom tests', () => {
  it('assings random user name', () => {
    // const userName = returned username after add client username
    expect(client.username).toEqual('user1');

  });

});
